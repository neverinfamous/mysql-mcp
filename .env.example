# Database
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=password
MYSQL_DATABASE=testdb
# Connection Pool
MYSQL_POOL_SIZE=10

# Tool Filtering (e.g., starter, essential, dev-power, ai-data, ai-spatial, dba-monitor, dba-manage, dba-secure, base-core, base-advanced, codemode, ecosystem, router, -core)
MYSQL_MCP_TOOL_FILTER=starter,codemode

# Server
MYSQLMCP_PORT=3001
MCP_HOST=localhost
MCP_ENABLE_HSTS=true
TRUST_PROXY=true
MCP_METRICS_EXPORT=true
ALLOWED_IO_ROOTS=C:/temp,/tmp
MCP_AUTH_TOKEN=
METADATA_CACHE_TTL_MS=30000
CODE_MODE_MAX_RESULT_SIZE=102400

# Audit Logging (Optional)
AUDIT_LOG_PATH=./audit.jsonl
AUDIT_LOG_MAX_SIZE=10485760
AUDIT_REDACT=true
AUDIT_READS=false
AUDIT_BACKUP=true
AUDIT_BACKUP_DATA=false
AUDIT_BACKUP_MAX_SIZE=52428800

# OAuth
OAUTH_ENABLED=false
OAUTH_ISSUER=http://localhost:8080/realms/mysql-mcp
OAUTH_AUDIENCE=mysql-mcp
OAUTH_JWKS_URI=
OAUTH_CLOCK_TOLERANCE=60
