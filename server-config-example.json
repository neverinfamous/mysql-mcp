{
  "_comment_usage": "Load this configuration by running: mysql-mcp --config path/to/server-config-example.json. Config Priority: CLI Flags > Environment Variables > Config File > Defaults",
  "_comment_priority": "For database connections and OAuth settings, the entire array/object must come from a single source to prevent mixing environments (e.g. CLI databases will fully override FILE databases).",
  "_comment_docker": "Docker mapping requirements: set host to 0.0.0.0 and adjust allowedIoRoots to match mounted volumes.",
  "host": "127.0.0.1",
  "port": 3000,
  "_comment_transport": "Defaults to stdio. Use http for docker-compose configurations.",
  "transport": "stdio",
  "enableHSTS": false,
  "trustProxy": false,
  "_comment_stateless": "MCP v2 stateless HTTP mode using NodeStreamableHTTPServerTransport over /mcp",
  "stateless": false,
  "authToken": "your-secure-auth-token",
  "metricsExport": "prometheus",
  "cors": {
    "origins": [
      "https://trusted-client.example.com"
    ],
    "allowCredentials": true
  },
  "toolFilter": "codemode",
  "allowedIoRoots": [
    "C:/temp/mcp-sandbox",
    "/tmp/mcp-sandbox"
  ],
  "auditConfig": {
    "enabled": true,
    "logPath": "/var/log/mysql-mcp/mcp-audit.jsonl",
    "redact": true,
    "auditReads": false,
    "maxSizeBytes": 10485760,
    "backup": {
      "enabled": true,
      "includeData": false,
      "maxDataSizeBytes": 1048576
    }
  },
  "_comment_databases": "Only the first database in the array is currently connected/supported by the MySQL adapter.",
  "databases": [
    {
      "type": "mysql",
      "host": "localhost",
      "port": 3306,
      "username": "mcp_user",
      "password": "secure_password",
      "database": "testdb",
      "options": {
        "ssl": true
      },
      "pool": {
        "connectionLimit": 10,
        "_comment_acquireTimeout": "recommended lower value for tight integration",
        "acquireTimeout": 10000,
        "waitForConnections": true,
        "queueLimit": 0
      }
    }
  ],
  "oauth": {
    "enabled": false,
    "issuer": "https://auth.example.com",
    "audience": "mysql-mcp-client",
    "jwksUri": "https://auth.example.com/.well-known/jwks.json",
    "clockTolerance": 60
  },
  "_comment": "Many configuration options can also be specified natively as camelCase properties. REDIS_URL, CODEMODE_ISOLATION, PROXYSQL_HOST, PROXYSQL_PORT, PROXYSQL_USER, PROXYSQL_PASSWORD, MYSQL_ROUTER_URL, MYSQL_ROUTER_USER, MYSQL_ROUTER_PASSWORD, MYSQL_ROUTER_API_VERSION, MYSQL_ROUTER_INSECURE, MYSQLSH_PATH, and LOG_LEVEL features, as well as CODEMODE_MAX_RESULT_SIZE, METADATA_CACHE_TTL_MS, MCP_RATE_LIMIT_MAX, CODEMODE_RATE_LIMIT_MAX, MYSQL_XPORT, DD_API_KEY, DD_APP_KEY, DD_HOSTNAME, DD_SITE, DD_EXTRA_PERFORMANCE_METRICS, and OTEL_EXPORTER_OTLP_ENDPOINT are injected via environment variables."
}
