{
  "host": "127.0.0.1",
  "port": 3000,
  "transport": "stdio",
  "enableHSTS": false,
  "trustProxy": false,
  "toolFilter": "starter",
  "allowedIoRoots": [
    "C:/temp/mcp-sandbox",
    "/tmp/mcp-sandbox",
    "/app/data"
  ],
  "auditConfig": {
    "enabled": true,
    "logPath": "/tmp/mcp-sandbox/audit.jsonl",
    "redact": true,
    "auditReads": false,
    "maxSizeBytes": 10485760,
    "backup": {
      "enabled": true,
      "includeData": false,
      "maxDataSizeBytes": 1048576
    }
  },
  "databases": [
    {
      "type": "mysql",
      "host": "localhost",
      "port": 3306,
      "username": "mcp_user",
      "password": "secure_password",
      "database": "testdb",
      "pool": {
        "connectionLimit": 10,
        "_comment_acquireTimeout": "recommended lower value for tight integration",
        "acquireTimeout": 10000,
        "waitForConnections": true,
        "queueLimit": 0
      }
    }
  ],
  "oauth": {
    "enabled": false,
    "issuer": "https://auth.example.com",
    "audience": "mysql-mcp-client",
    "jwksUri": "https://auth.example.com/.well-known/jwks.json",
    "clockTolerance": 60
  },
  "_comment": "redisUrl, codeModeIsolation, proxySql, mysqlRouter, mysqlsh, and logLevel features, as well as CODE_MODE_MAX_RESULT_SIZE, METADATA_CACHE_TTL_MS, MCP_RATE_LIMIT_MAX, CODEMODE_RATE_LIMIT_MAX, MCP_REQUEST_TIMEOUT, MCP_HEADERS_TIMEOUT, MCP_AUTH_TOKEN, and MYSQL_XPORT are injected via environment variables only (REDIS_URL, etc.) and are silently ignored by the JSON/YAML file parser."
}
