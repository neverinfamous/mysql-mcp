# .npmrc — mysql-mcp supply chain hardening
#
# Disable automatic execution of install scripts from transitive
# dependencies to prevent arbitrary code execution during npm/pnpm install.
#
# See: https://docs.npmjs.com/cli/v10/using-npm/scripts#best-practices

ignore-scripts=true
