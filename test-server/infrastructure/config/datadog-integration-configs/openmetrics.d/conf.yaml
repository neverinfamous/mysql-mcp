init_config:

instances:
  - openmetrics_endpoint: http://host.docker.internal:3000/metrics
    namespace: mysql_mcp
    tags:
      - service:mysql_mcp
      - env:development
    min_collection_interval: 15
    timeout: 10
    metrics:
      - '.*'
