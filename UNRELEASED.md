# Unreleased

## Added

- **Introspection**: Tools for dependency mapping, topological sort, schema snapshots, and risk assessment.
- **Migration**: Tools for tracking, applying, and rolling back schema versions.
- **Insights Subsystem**: `mysql_append_insight` tool and `mysql://insights` resource for session-based business insights.
- **Token Estimation**: `_meta.tokenEstimate` heuristic (4 bytes/token) in all tool responses and Code Mode metrics.
- **Audit Observability**:
  - Activated logging via `--audit-log` and `--audit-backup` CLI flags.
  - Exposed `getAuditInterceptor()` on `DatabaseAdapter` for Code Mode API integration.
  - Wired `AuditInterceptor` through sandbox operations to close audit blindspots.
  - Ported 51-test audit unit test suite.
- **Benchmarks**: Code Mode performance and throughput benchmark suite.
- **Help Architecture**: Dynamically registered group-specific help resources (`mysql://help/{group}`).
- **Error Mapping**: `utils/error-suggestions.ts` to map MySQL error codes to actionable suggestions.
- **Connection Pool**: `initializationSql` config to execute setup queries once per checkout.
- **Cluster Recovery**: `scripts/reboot-cluster.ps1` utility for recovering InnoDB Clusters from complete outages.

## Changed

- **Schema Modularity**: Decentralized monolithic `types.ts` into modular, group-specific schemas for better maintainability.
- **Sandbox Hardening**: Hardened `MysqlApi` bindings in Code Mode to stub write-methods in `readonly` mode and auto-return the last expression.
- **Instructions**: Replaced monolithic 53KB server instructions with a ~634 char summary + on-demand MCP resources.
- **Events Syntax**: Simplified schema definitions to accept standard MySQL syntax strings.
- **Dependencies**: Updated core dependencies (`@modelcontextprotocol/sdk`, `vitest`, `eslint`, `typescript`, `mysql2`, `jose`, `zod`, `typescript-eslint`, and `minimatch` in Dockerfile).
- **Dependency Updates**: Bumped `eslint` to 10.3.0, `globals` to 17.6.0, and `zod` to 4.4.2.
- **Token Optimization**:
  - Reduced default limits to 3 for `mysql_query_stats`, `mysql_slow_queries`, `mysql_index_usage`, and to 5 for `mysql_export_table`.
  - Reduced default limit to 5 for `mysql_binlog_events` to prevent payload bloat and ensure architectural consistency.
  - Defaulted `mysql_optimizer_trace` and `partition_info` to `summary: true`.
  - Defaulted `ShowProcesslistSchema`, `ShowStatusSchema`, and `ShowVariablesSchema` to prevent payload bloat.

## Removed

- Monolithic `ServerInstructions.ts` and dynamic group-filtering utilities.

## Fixed

- **Core**: Remediated missing `metrics.tokenEstimate` payloads in success and domain error paths across all core tools (`mysql_read_query`, `mysql_write_query`, `mysql_list_tables`, `mysql_describe_table`, `mysql_create_table`, `mysql_drop_table`, `mysql_get_indexes`, `mysql_create_index`) by integrating `withTokenEstimate` wrappers. Fixed duplicate "Validation error: " prefixes in Zod error messages for schema refinement rules.
- **Error Handling**: Fixed Zod error duck-typing in `formatHandlerErrorResponse` to correctly intercept validation errors regardless of module duplication. Fixed `strict-boolean-expressions` ESLint violations in error helpers. Unified reporting across all tool groups to adhere to the `ErrorResponse` schema (`{ success: boolean }`). Eliminated legacy property leakages, standardized Zod error formats, fixed `MySQLMcpError` property stripping, and ensured that all structured error responses consistently include `metrics.tokenEstimate` payloads.
- **Validation & Coercion**: Applied the Split Schema and SchemaBase patterns across all tool groups. This ensures missing required parameters and invalid types (e.g., numeric limits) are properly coerced via `z.unknown()` or gracefully return structured handler errors instead of raw MCP exceptions.
- **Backup**: Fixed `DATETIME` ISO 8601 string parsing for MySQL strict mode in `importData`. Added `.min(1)` constraint to `tables` array in `mysql_create_dump`. Remediated missing `metrics.tokenEstimate` payloads in success paths and domain errors across all backup tools (`mysql_export_table`, `mysql_import_data`, `mysql_create_dump`, `mysql_restore_dump`, `mysql_audit_list_backups`, `mysql_audit_restore_backup`, `mysql_audit_diff_backup`) by enforcing `withTokenEstimate` wrappers and `formatHandlerErrorResponse`.
- **Admin DDL**: Switched to `rawQuery` to prevent `mysql2` from corrupting multi-row array responses.
- **Admin**: Remediated missing `metrics.tokenEstimate` payloads in success paths and domain errors across all admin tools (`mysql_optimize_table`, `mysql_analyze_table`, `mysql_check_table`, `mysql_repair_table`, `mysql_flush_tables`, `mysql_kill_query`, `mysql_append_insight`) by integrating `withTokenEstimate` into the return paths and enforcing `formatHandlerErrorResponse`. Updated test assertions to properly validate against the newly added metrics payloads.
- **Cluster**: Fixed auto-recovery by persisting `group_replication_start_on_boot=ON` across restarts. Remediated missing `metrics.tokenEstimate` payloads in success and domain error paths across all cluster tools (`mysql_cluster_status`, `mysql_cluster_instances`, `mysql_cluster_topology`, `mysql_cluster_router_status`, `mysql_cluster_switchover`).
- **Code Mode**: Replaced `_meta.tokenEstimate` with `metrics.tokenEstimate` in all group-level and top-level `help()` responses in `MysqlApi.ts` to ensure consistency with structured metrics payload validation.
- **Docstore**: Migrated `doc_find` to use `parseDocFilter` for query parity. Remediated missing `metrics.tokenEstimate` payloads in success and domain error paths across all document tools (`mysql_doc_list_collections`, `mysql_doc_create_collection`, `mysql_doc_drop_collection`, `mysql_doc_collection_info`, `mysql_doc_find`, `mysql_doc_add`, `mysql_doc_modify`, `mysql_doc_remove`, `mysql_doc_create_index`) by integrating `withTokenEstimate` wrappers. Fixed `mysql_doc_find` to automatically merge missing `_id` fields into returned documents for tables where `_id` is stored strictly as a column. Fixed Zod parameter parsing in `mysql_doc_modify` and `mysql_doc_remove` to support `criteria` as an alias for `filter` and `update` as an alias for `set`. Fixed `parseDocFilter` to properly support stringified JSON criteria aliases by parsing and extracting the field-value pairs.
- **Fulltext**: Removed the hardcoded `id` column requirement from the SELECT clause for FULLTEXT operations. Fixed `Incorrect arguments to mysqld_stmt_execute` regression by removing prepared statement parameterization for the `LIMIT` clause in search operations. Added missing `metrics.tokenEstimate` properties to index creation and drop payloads. Mapped raw MySQL `syntax error, unexpected` exceptions to graceful `Invalid search syntax` validation errors during BOOLEAN mode searches. Reduced default limit from 10 to 5 in search operations (search, boolean) and to 3 for expand search to prevent payload token bloat.
- **Introspection**: Fixed circular dependency detection and implemented active `maxDepth` traversal filtering in `dependency_graph`. Fixed Zod schema validation regression for `limit` preprocessing in schema snapshots and dependency graphs. Restored missing stats tools from previous test sessions. Added missing `metrics.tokenEstimate` properties to all introspection payloads (`dependencyGraph`, `topologicalSort`, `cascadeSimulator`, `schemaSnapshot`, `constraintAnalysis`, `migrationRisks`).
- **JSON**: Fixed parameter visibility in `json_validate` and handled malformed JSON/empty strings gracefully. Added `path` alias for `paths` in `json_remove`. Implemented missing `where` and `limit` clauses for `json_contains` and `json_keys`. Added missing `metrics.tokenEstimate` payloads across all operations, standardized domain error wrapping via `formatHandlerErrorResponse`, and rectified missing schema requirements (`id`, `idColumn`) for `json_get` and `json_update`. Added `limit` support to `json_extract` to strictly bound result set payload token consumption.
- **Migration**: Fixed read-only mode false positives using `executeWriteQuery`. Fixed prepared statement constraint errors in `migration_history`. Enforced strict validation of schema hashes and out-of-order execution. Added missing `metrics.tokenEstimate` payloads across all migration operations, standardizing token payload boundaries for domain errors and hash validations. Corrected test prompt references to the `_mcp_schema_versions` tracking table.
- **Optimization**: Fixed domain error reporting in `index_recommendation` and `force_index`. Surfaced `rewrittenQuery` in `query_rewrite`. Fixed EXPLAIN payload optimization by defaulting to `TREE` format. Added missing `metrics.tokenEstimate` payloads across all optimization operations.
- **Roles**: Fixed parameter visibility regressions in MCP caused by Zod wrappers. Supported revoking privileges from roles in `role_revoke`. Added missing `metrics.tokenEstimate` payloads to all success and error paths.
- **Router**: Fixed `router_route_health` to return graceful health object on 500 errors for offline routes.
- **Schema**: Fixed missing `metrics.tokenEstimate` payloads in success paths and domain errors across all schema tools by explicitly injecting token estimates into success payloads and enforcing `formatHandlerErrorResponse`. Fixed DDL operations to correctly return `{ success: true, skipped: true }` when conditions (`ifExists`, `ifNotExists`) are met. Implemented missing `mysql_drop_view` tool to achieve complete feature parity.
- **Security**: Enforced `.min(1)` constraint on `password` parameter in `password_validate` to reject empty strings. Fixed missing `metrics.tokenEstimate` payloads in success paths and domain errors across all security tools by centralizing metrics injection via `withTokenEstimate` and enforcing `formatHandlerErrorResponse`. Reduced default limit in `mysql_security_audit` from 20 to 10 to prevent payload bloat.
- **Spatial**: Fixed `spatial_create_index` emitting table not found errors on missing columns. Fixed WKT round-tripping for SRID 4326. Optimized `mysql_spatial_buffer` payload by removing massive GeoJSON generation. Added missing `metrics.tokenEstimate` payloads to all success and error paths. Extracted inline Zod schemas from handler files into the decentralized `adapters/mysql/schemas/spatial.ts` to ensure architectural consistency with the SchemaBase pattern. Defaulted `nullable` to `false` in `SpatialColumnSchema` to prevent SPATIAL index creation failures on nullable geometry columns.
- **Stats**: Enforced numeric type checking, minimum/maximum bucket counts in histogram/distribution. Fixed variable interpolation in advanced error handlers. Remediated missing `metrics.tokenEstimate` payloads across all 20 stats tools (`descriptive`, `percentiles`, `distribution`, `timeSeries`, `sampling`, `correlation`, `regression`, `histogram`, `rowNumber`, `rank`, `lagLead`, `runningTotal`, `movingAvg`, `ntile`, `hypothesis`, `outliers`, `topN`, `distinct`, `frequency`, `summary`) by importing and applying `withTokenEstimate` in all 5 submodules (`descriptive.ts`, `comparative.ts`, `window.ts`, `hypothesis.ts`, `outlier.ts`, `advanced.ts`). Fixed `outliers` tool which delegated to internal helpers (`detectIqrOutliers`, `detectZScoreOutliers`) without token wrapping. Fixed `lagLead` direction parameter schema to use enum literals ("lag", "lead"). Fixed `histogram` tool to use `update: true` to seed histogram before reading. Corrected erroneous `withTokenEstimate` wrapper on ToolDefinition factory returns (script-introduced regression) across all 5 submodules. Removed `withTokenEstimate` from data-mapping `.map()` callbacks in `distribution`, `summary`, and `hypothesis` grouped results. Fixed misleading error messages for nonexistent columns in `correlation`, `regression`, and `percentiles` — now correctly distinguishes between "column not found" and "column not numeric". Eliminated SQL query exposure from error responses by replacing `formatHandlerErrorResponse` with `withTokenEstimate` in all non-ZodError catch paths across `comparative.ts`, `window.ts`, `hypothesis.ts`, `outlier.ts`, and `advanced.ts` (SQL was leaking via `MySQLMcpError.details.sql` from the connection pool layer). Fixed `stats.histogram` to return `{success: true, exists: false}` when querying a column without an existing histogram instead of returning a domain error. Fixed window function tools (`rowNumber`, `rank`, `lagLead`, `runningTotal`, `movingAvg`, `ntile`) to catch 'Unknown column' MySQL errors and sanitize them into structured validation messages instead of leaking raw SQL exceptions.
- **Sys Schema**: Registered `mysql.sys` as a direct API alias for `mysql.sysschema` in Code Mode bindings, supporting intuitive shorthand calls. Remediated missing `metrics.tokenEstimate` payloads across all sysschema tools (`user_summary`, `host_summary`, `statement_summary`, `wait_summary`, `io_summary`, `schema_stats`, `innodb_lock_waits`, `memory_summary`) by enforcing `withTokenEstimate` wrappers in success paths and early domain errors. Updated test suite assertions to validate the newly included token metrics.
- **Shell**: Extended language validation to support JavaScript and Python. Fixed `dump_tables` dry run configuration. Fixed Windows path resolution using `path.resolve`.
- **ProxySQL**: Added missing `version` and `uptime` properties to `proxysql_status` response.
- **Text**: Added `targetCharset` alias mapping for `charset` parameter in `collationConvert` schema validation to improve agent tool-calling resilience. Remediated missing `metrics.tokenEstimate` payloads across all text tools (`regexp_match`, `like_search`, `soundex`, `substring`, `concat`, `collation_convert`) by enforcing `withTokenEstimate` wrappers in success paths.
- **Tests**: Remediated benchmark timing assertions, fixed `vitest bench` watch-mode hangs, and gracefully skipped E2E write tests in read-only mode.
- **Performance**: Fixed integer overflow bug in `detectQueryAnomalies` caused by `performance_schema` timer wraps. Added strict limits truncation for `sys` schema performance tools (`sysStatementSummary`, `sysWaitSummary`, `sysIOSummary`) to prevent token payload blowouts. Further optimized payload bloat by reducing `LIMIT`s to 20 for `queryStats` and `threadStats` and truncating `DIGEST_TEXT` to 150 characters with `LIMIT 10` in `detectQueryAnomalies`. Optimized JSON EXPLAIN payload size by stripping verbose `used_columns` and deep `cost_info` details to prevent token limits from being exceeded during complex joins.
- **Events**: Fixed `mysql_scheduler_status` missing `status` field and `mysql_event_status` missing `event` object in response payloads. Remediated missing `metrics.tokenEstimate` payloads in success and domain error paths across all events tools (`mysql_event_create`, `mysql_event_alter`, `mysql_event_drop`, `mysql_event_list`, `mysql_event_status`, `mysql_scheduler_status`) by integrating `withTokenEstimate` wrappers.
- **Transactions**: Fixed parameter alias parsing in `mysql_transaction_begin` and `mysql_transaction_execute` to correctly map `isolation_level` to `isolationLevel`. Added missing `metrics.tokenEstimate` payloads to all success and error paths by integrating `withTokenEstimate` and `formatHandlerErrorResponse`.
- **Monitoring**: Fixed missing `metrics.tokenEstimate` payloads across all monitoring tools (`mysql_show_processlist`, `mysql_show_status`, `mysql_show_variables`, `mysql_innodb_status`, `mysql_replication_status`, `mysql_pool_stats`, `mysql_server_health`) by explicitly injecting token estimates into all success payloads and unifying domain validation errors via `formatHandlerErrorResponse`. Fixed `innodbStatus` payload bloat by dynamically truncating raw output to 1000 characters when `summary` is explicitly set to `false`. Renamed `poolStats` response property from `stats` to `poolStats` and `serverHealth` from `health` to `serverHealth` to ensure architectural consistency. Fixed raw `TypeError` object throwing by switching `adapter.rawQuery` to `adapter.executeQuery` in `showStatus` and `showVariables` implementations and updating unit test mocks.
- **Partitioning**: Fixed missing `metrics.tokenEstimate` payloads in success and domain error paths across all partitioning tools (`mysql_partition_info`, `mysql_add_partition`, `mysql_drop_partition`, `mysql_reorganize_partition`).
- **Replication**: Remediated missing `metrics.tokenEstimate` payloads in success paths for all replication tools (`mysql_master_status`, `mysql_slave_status`, `mysql_binlog_events`, `mysql_gtid_status`, `mysql_replication_lag`) by integrating `withTokenEstimate` wrappers.

## Security

- **Scope Enforcement**: Fixed a vulnerability where HTTP transports validated tokens but bypassed tool-specific scope enforcement.
- **Dependency Patches**: Updated `hono` to `4.12.9` to patch SSE control field, cookie attribute injection, and prototype pollution. Updated `flatted`, `path-to-regexp`, `picomatch`, `express-rate-limit`, `@hono/node-server`, `tar`, and `minimatch` to patch various vulnerabilities.
- **CI/CD Hardening**: Pinned all GitHub Actions by SHA, added TruffleHog + Gitleaks secret scanning, and integrated SLSA Build L3 attestation via `--provenance`. Repositioned Trivy vulnerability scanning to run before image pushes.

### Certified

- `events` tool group verified for 100% adherence to `{success: boolean, error?: string}` contract and `metrics.tokenEstimate` instrumentation via MCP direct calls.
