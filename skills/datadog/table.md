| Skill | Description |
| --- | --- |
| `adversarial-skill-builder` | Guide for creating, evaluating, and refining agent skills (.md files with YAML frontmatter). Use this skill whenever you are creating a new skill, improving an existing skill, reviewing skill quality, writing skill descriptions, or when the user asks about skill structure, progressive disclosure, or best practices for agent instructions. Also trigger on: "turn this into a skill", "make a skill for X", "evaluate this skill", or "review this third-party skill". |
| `auth-identity` | Standards for Authentication and Identity management. Use when configuring OAuth, JWTs, sessions, RBAC (Role-Based Access Control), or integrating auth providers (Auth0, Clerk, NextAuth). Do NOT trigger for generic user profile display, database schema for users without auth flows, or generic session storage (use redis skill instead). SECURITY: Implementing auth requires strict fallback logic. Never fail-open. Handle token expiration and revocation gracefully. |
| `bun` | Master the Bun all-in-one toolkit — runtime, package manager, test runner, and bundler. Use when writing, running, testing, or bundling TypeScript/JavaScript with Bun. Triggers on "bun", "bunx", "bun add", "bun build", "bun.lockb", "bun install", "bun test", "bun run". Do NOT trigger for generic "write tests" unless in a known Bun project context. |
| `chrome-devtools` | Guide for using the chrome-devtools-plugin for Puppeteer-based browser automation, testing, DOM inspection, and accessibility (a11y) audits. Triggers on: "puppeteer", "chrome devtools", "browser automation", "DOM inspection", "WebMCP", "agentic web", "a11y", "accessibility". Do NOT trigger for standard API testing or static scraping without JS. |
| `cli-development` | Master guide for developing top-of-the-line, production-grade Command Line Interfaces (CLIs) in TypeScript/Node.js. Use this skill when designing, building, or refactoring CLI tools, scripts, or terminal applications. Triggers on "CLI", "command line tool", "terminal app", "commander", "clack", "yargs", "util.parseArgs", "stdout/stderr". Do NOT trigger for standard bash scripting or simple one-off npm scripts. |
| `cloudflare` | Comprehensive Cloudflare platform skill covering Workers, Pages, storage (KV, D1, R2), AI (Workers AI, Vectorize, Agents SDK), feature flags (Flagship), networking (Tunnel, Spectrum), security (WAF, DDoS), and infrastructure-as-code (Terraform, Pulumi). Use for any Cloudflare development task. Biases towards retrieval from Cloudflare docs over pre-trained knowledge. |
| `docker` | Production-grade Docker and container best practices. Use when containerizing apps with Docker,  writing Dockerfiles, or managing Docker Compose environments. |
| `drizzle-orm` | Drizzle ORM best practices. Use when configuring databases, designing schemas, writing queries, or handling migrations with Drizzle ORM. Works with PostgreSQL, MySQL, SQLite, and Cloudflare D1. NOT for database setup without an explicit engine. Require engine specification. Use for ORM-managed migrations. |
| `github-ecosystem` | Consolidated hub for all GitHub integrations, workflows, and CLI tools. Use this skill when: - Writing or debugging GitHub Actions (CI/CD pipelines, YAML workflows) - Interacting with the GitHub Commander (issues, PRs, comments) - Using the GitHub Copilot CLI (`gh copilot`) for explanations or validations - Managing repository settings, branch protections, or GitHub-specific features Do NOT trigger for non-GitHub CI (like GitLab, Jenkins, AWS). ROUTING RULE: If the request is specifically about Actions YAML, load `github-actions`. If it's about the CLI, load `github-commander`. |
| `github-repo-setup` | Reusable scaffold for public TypeScript/Node.js repositories under a target organization. Use when creating a new GitHub repository to generate all community standards, CI/CD, config files, labels, and topics in one pass. |
| `hono` | Hono framework best practices. Use when building, reviewing, or debugging Hono applications, especially on Cloudflare Workers. Covers routing, middleware, context (c), and RPC patterns. Triggers on: "hono", "edge runtime", "Workers", "rpc", "middleware". Do NOT trigger for generic 'build a server' requests unless Hono or Cloudflare Workers is explicitly requested. |
| `mcp-builder` | Guide for creating high-quality MCP (Model Context Protocol) servers that enable LLMs to interact with external services through well-designed tools. Use when building MCP servers, designing tool schemas, implementing error handling, adding OAuth/HTTP transport, or connecting AI to external APIs using Node/TypeScript. Also use when reviewing existing MCP server code for best practices. |
| `mysql` | Use when designing, querying, or managing a MySQL or MariaDB database. Enforces enterprise production rules for query safety (strict parameterization),  connection pooling, and strict schema configurations (STRICT_TRANS_TABLES). MUST-ASK: Require explicit database engine name before triggering. Never guess. Do NOT trigger for generic "set up a database" requests. NOT for Postgres or SQLite. NOT for ORM-managed migrations. |
| `nextjs` | Consolidated hub for React and Next.js development. Use this skill when: - Building or refactoring React components and hooks - Working with Next.js App Router, routing, or layouts - Implementing Next.js caching (`use cache` directive, Cache Components, PPR) - Upgrading Next.js versions or migrating from Pages to App Router - Seeking general frontend React best practices Do NOT trigger for plain JavaScript/TypeScript questions unrelated to React or Next.js. |
| `nodejs` | Node.js core runtime standards. Use when working with native Node.js APIs like streams, buffers, child_process, worker_threads, filesystem, or event loop tuning. Do NOT trigger for generic 'build a server' requests unless the platform/language is explicitly specified, or for framework-layer requests (Express, Fastify, NestJS) unless raw Node.js APIs are explicitly involved. |
| `opentelemetry` | Observability standards using OpenTelemetry. Use when instrumenting applications for distributed tracing, metrics, and structured logging. NOT for Cloudflare Workers observability (use workers-best-practices). NOT for performance optimization - use adversarial-performance or web-perf. SECURITY: Ensure sensitive PII/secrets are scrubbed before logging. |
| `pattern-matching` | Master guide for implementing pattern matching across TypeScript and Python. Use when writing conditional logic, branching, parsing state, or replacing complex `switch` and `if/else` chains. Triggers on "pattern matching", "ts-pattern", "Python match case", "switch expressions". |
| `postgres` | Use when designing, querying, or managing a PostgreSQL database. Enforces enterprise production rules for advanced querying, composite indexing,  JSONB data handling, and strict optimization patterns (avoiding N+1). MUST-ASK: Require explicit database engine name before triggering. Never guess. Do NOT trigger for generic "set up a database" requests. NOT for SQLite or MySQL. NOT for ORM-managed migrations. |
| `powershell` | Master PowerShell scripting and automation on Windows. Use this skill when writing `.ps1` scripts, executing shell commands via `pwsh`, dealing with Windows environment variables, or handling complex quoting, pipes, and object manipulation in PowerShell. Crucial for Windows-specific orchestration. Do NOT trigger for standard bash scripting or WSL unless explicitly bridging to pwsh. |
| `python` | Master modern Python development with production-grade tooling and idioms. Use when writing Python code, configuring project structure, managing dependencies with uv, linting with ruff, adding type hints, writing pytest tests, or building FastAPI/Django/Flask applications. Do NOT trigger for generic 'build a server' requests unless the platform/language is explicitly specified as Python (e.g. FastAPI, Django, Flask). |
| `redis` | Redis best practices. Use when configuring caching strategies, connection pooling, handling TTLs (Time-To-Live), or managing Redis data structures (Hash, Set, List) in Node/TypeScript projects. MUST-ASK: Require explicit database engine name before triggering. Never guess. Do NOT use for generic "set up a database" or "create a DB" requests without clarifying. |
| `shadcn-ui` | Use when adding, customizing, or structuring shadcn/ui components in a project. Also use when dealing with `components.json`, Tailwind configuration for shadcn, or component registries. |
| `sqlite` | Enforced meta-cognitive rules and production configurations for SQLite development. |
| `tailwind-css` | Master Tailwind CSS v4 with its CSS-first configuration paradigm. Use when writing utility classes, configuring design tokens via @theme, implementing dark mode, migrating from v3, or integrating with React/Vue/Svelte. CAUTION: Emitting extensive Tailwind utility classes consumes significant token space; prioritize semantic component extraction. |
| `testing-standards` | Consolidated hub for all testing frameworks and standards. Use this skill when: - Writing or debugging E2E tests with Playwright - Writing unit/integration tests with Vitest or Jest - Setting up testing environments, mocks, or fixtures - Asking about UI testing, component testing, or test coverage |
| `typescript` | Comprehensive enterprise-grade TypeScript operational guide. Use when configuring TS projects, writing complex generics, or enforcing strict type safety. Triggers on "TypeScript", "tsconfig.json", "generics", "type errors", "strict mode". SECURITY: Do not bypass type checks with 'any' or '@ts-ignore'. |
| `wsl` | Master Windows Subsystem for Linux (WSL) development and scripting. Use this skill when writing scripts for WSL, configuring WSL environments, crossing the Windows/Linux OS boundary, or optimizing file system performance in WSL. Do NOT trigger for standard native Windows PowerShell scripting or pure Linux server administration without WSL context. |
| `zod` | Zod schema validation standards. Use when defining schemas, parsing user input, transforming data, or integrating type-safe boundaries in API endpoints and configurations. |
