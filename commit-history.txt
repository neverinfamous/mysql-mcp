Fix docstore tools to return structured validation errors instead of raw unknown errors.

- Replaced `throw new Error()` with `throw new ValidationError()` in `parseDocFilter`.
- Invalid JSON paths and filters now correctly map to `VALIDATION_ERROR` instead of `UNKNOWN_ERROR`.
- Verified all lifecycle and edge case operations in Code Mode.
