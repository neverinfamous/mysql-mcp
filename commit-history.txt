Fixed schema mappings for security tools. Made value and type required in MaskDataSchemaBase to fix a Split Schema violation where they were required downstream but not exposed to the MCP client. Added userName alias to UserPrivilegesSchemaBase and preprocessed it into user to match what was being tested. Also corrected hallucinated API signatures in test-codemode-security-audit.md.
